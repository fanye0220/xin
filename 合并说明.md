# miu_v39 更新与合并说明（基于 9月最新miu 9-25 最新重大更新版）

在「9月最新miu (6)」及前期版本基础上完成完整功能整合、构建与打包：

## 本次更新（v39）核心特性与修复

1. **角色卡版本迭代与溯源中心 (src/components/CharacterVersionsSection.tsx, src/components/CharacterDetail.tsx, src/lib/db.ts)**
   - **全新「版本迭代」专属面板**：在角色详情页中新增「版本迭代」Tab，并支持从卡片版本号及元数据区一键直达溯源中心。
   - **版本快照保存与归档**：支持为角色卡创建历史版本快照（包含卡面/头像、卡片数据、版本名称、更新说明、修改时间），支持对比版本差异。
   - **历史版本一键回滚/恢复**：可随时将当前角色卡一键回滚到任意历史快照版本，或将历史版本另存/派生为新角色卡。
   - **版本历史卡片独立导出与下载**：支持单独下载/分享任意历史版本的 PNG 卡片或 JSON 数据。

2. **云端同步与完整备份全面支持版本历史 (src/lib/cloudDrive.ts, src/lib/drive.ts)**
   - **云端卡库同步版本历史**：上传卡片到 Google 云端时，自动将 ersionHistory 快照打包进 版本历史/ 目录并写入 studio_meta.json；从云端下载或全量同步时无损恢复全部历史快照与卡面。
   - **完整备份与恢复**：Google Drive 完整备份与恢复流程全面兼容 ersionHistory 快照和对应的卡面 PNG，多端同步完全不丢版本记录。

3. **导入归档自动识别版本历史与 Studio 元数据 (src/components/ImportModal.tsx)**
   - 导入 ZIP 压缩包或文件夹时，智能识别 版本历史/、ersions/、history/ 目录以及 studio_meta.json，自动将历史版本快照绑定至对应的角色主卡，无需手动整理。

4. **云端卡库直接下载至本地 (src/components/CloudSyncTab.tsx)**
   - 按钮文案保持为「下载」，点击直接调用底层下载/保存接口保存至设备或拉起保存，无需冗余步骤。

5. **快速回复绑定弹窗偏好记忆与主题适配 (src/components/ConfirmBindQRModal.tsx, src/components/ImportModal.tsx)**
   - 绑定确认弹窗的「绑定后将独立 QR 移至回收站」选项自动记忆至 localStorage，导入界面的同名卡自动归类选项也优化了浅色主题下的视觉勾选框体验。

6. **底层真实时间戳解析与导入时间修复 (src/lib/fileDate.ts, src/components/ImportModal.tsx, src/components/CharacterDetail.tsx)**
   - 支持从 PNG 	IME 块、EXIF 时间头以及卡片内部嵌套元数据中精准提取图片原始生成时间。
   - 彻底解决 Android SAF 文件选择器导入时将文件拷贝到临时缓存导致 lastModified 被伪造为导入时间的问题（智能识别 120s 内的临时缓存时间并自动提取真实时间）。
   - 详情页与列表按修改时间排序完全基于真实时间精准展示与升降序。

7. **主页与分类内卡片直接拖拽移入文件夹 (src/components/CharacterList.tsx)**
   - 拖拽卡片至文件夹上方时触发蓝色高亮及「松手移入文件夹」气泡，松手即可直接归类。

8. **跨层级目录翻页位置独立记忆 (src/components/CharacterList.tsx)**
   - 引入 olderPageMemory，在主页、子文件夹、角色详情之间返回切换时，精准记忆每个目录当前的页码（如第 5 页），不再跳回第 1 页。

9. **跨版本关键特性完整保留与兼容**
   - **会话导入增强 (src/lib/chatParse.ts, src/components/CharacterChatsSection.tsx)**：完整保留多行 TXT 会话解析（支持中英文冒号 ：/ :）、JSONL 逐行解析与 ZIP 压缩包批量导入。
   - **详情页系统返回隔离与多级路径 (src/components/CharacterDetail.tsx, src/components/MoveToFolderModal.tsx)**：返回键优先退出内部浮层/详情，避免穿透退出文件夹。
   - **重复检测长路径自适应截断 (src/components/DuplicateDetector.tsx)**。

## 版本包说明
- **miu_v39**: 完整 Capacitor 项目工程（包含 android/ 目录与最新 dist/ 编译产物）。
- **miu_v39_web_干净版**: 纯净前端工程（纯 src/ 结构，无任何冗余根目录重复代码及缓存，轻量整洁）。
- **miu_v39_android_干净版**: Android Studio 干净构建包（已同步最新 Web 产物至 assets/public，无需 node 环境即可直接导入 Android Studio 打包 APK）。
