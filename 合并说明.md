# miu_v50 更新与合并说明（基于 9月最新miu 32 重复卡免删锁定与云端QR完整归档版）

在「9月最新miu (32)」及前期版本基础上完成全量特性合并、重复卡片免删锁定保护、云端 QR 独立归档导出、云端全维度数据指纹、生产构建打包：

## 本次更新（v50）核心特性与修复

1. **重复卡片免删锁定保护系统 (`src/components/DuplicateDetector.tsx`)**
   - **手动锁定/解锁**：在重复卡片清理页面中，点击右上角「锁定」按钮可将特定卡片设为「免删保护」状态。
   - **智能防误删**：已锁定的卡片无法被勾选、无法被单卡删除，在执行「保留并合并」或「批量删除」时会自动跳过锁定卡片（并提示跳过数量），彻底杜绝误删珍贵版本。

2. **云端卡库全面支持快速回复 (QR) 独立归档导出 (`src/lib/cloudDrive.ts`)**
   - **QR 独立归档**：在单卡打包上传至云端卡库（ZIP 格式）时，自动提取角色卡内绑定的快速回复设定（`extensions.quick_replies` / `tavern_qr_sets`），在 ZIP 归档根目录下同步生成标准格式的 `${safeName}_qr.json` 独立文件。
   - **数据完整无损**：确保云端备份同时完整封存角色卡、底图、差分头像、版本历史、备忘录、小剧场、聊天记录与 QR 快速回复数据。

3. **云端全维度数据指纹比对算法 (`src/lib/cloudDrive.ts`)**
   - **全面包含附加数据**：指纹比对范围完整纳入 `id`、`name`、`updatedAt`、`tags`、`folderPath`、`data`、`avatar`、`avatarHistory`、`versionHistory`、`memos`（包含剧场图片与置顶）、`chats`（对话条目与内容），确保任意细节变更均能精准触发云端同步。
   - **强制覆盖参数支持**：`uploadCharacterToCloud` 支持 `forceOverwrite` 参数，支持跳过哈希一致性检测直接强制覆盖云端文件。

4. **2K/3K 800px 超清缩略图生成器与版本升级缓存 (`src/lib/avatar.ts`, `src/lib/db.ts`)**
   - **800px 视网膜超清**：缩略图生成分辨率提升至 **800px（质量 0.90）**，在大屏手机、平板或高分辨率显示器上呈现极致细腻通透的画质。
   - **版本平滑升级机制**：`getCharacterThumb` 引入 `thumbVersion: 3` 标记检测，自动渐进式将本地历史低清缩略图在浏览时平滑无感地刷新为 800px 超清缩略图。

5. **云端路径智能解析与同名分类继承 (`src/lib/cloudDrive.ts`, `src/components/CloudSyncTab.tsx`)**
   - **独立解析引擎**：抽取封装 `resolveAppFolderFromCloudPath` 核心函数。
   - **自动剥离系统虚拟大类**：下载/恢复回 App 时，自动剥离云端专用的虚拟大类（`角色卡`、`工具区`、`聊天记录`）与工具二级分类，将真实用户文件夹精准还原回 App 本地。
   - **同名卡分类继承**：支持开启「同名卡自动跟随分类」后，自动继承本地已有同名卡的文件夹分类。

6. **封面裁剪器界面优化与 2:3 竖卡标准 (`src/components/CharacterList.tsx`)**
   - 统一固定为 2:3 标准竖卡比例，精简多余选项。
   - 适配移动端 Safe Area 安全区边距与响应式保存/取消按钮。

7. **IndexedDB 幽灵脏数据后台自动清除与启动秒开 (`src/lib/db.ts`, `src/App.tsx`)**
   - **`cleanupGhostCards` 自动清理**：启动后 500ms 异步后台检测数据库，精准扫描并清除无名称且无任何有效设定的无效幽灵卡，以及孤立的元数据与头像 Blob。
   - **启动秒开加速**：启动数据库迁移完毕后瞬间进入主页（`setIsMigrating(false)`），文件夹与幽灵卡清理全异步运行，启动体验极速顺滑。

8. **Web 端环境隔离与 Android 自动更新系统**
   - **Web 端纯净无扰**：自动隐藏设置中的「关于与更新」标签页，不触发任何更新检查弹窗。
   - **Android 在线更新**：保持对接 GitHub 仓库 `fanye0220/APP`，支持 3 秒自动静默检测与一键跳转 Releases 下载。

## 版本包说明
- **miu_v50**: 完整 Capacitor 项目工程（包含 android/ 目录与最新 dist/ 编译产物）。
- **miu_v50_web_干净版**: 纯净前端工程（纯 src/ 结构，~380KB，轻量整洁，无更新弹窗干扰）。
- **miu_v50_android_干净版**: Android Studio 干净构建包（已同步最新 Web 产物至 assets/public，可直接打包 APK）。
- **GitHub更新文件_方便上传**: 包含可直接上传到 GitHub 仓库根目录的 `version.json` 与发布指南。
