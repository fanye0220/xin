# miu_v42 更新与合并说明（基于 9月最新miu 9-26 视觉、封面与性能优化版）

在「9月最新miu (24)」及前期版本基础上完成完整功能合并、拖拽性能优化、生产构建与打包：

## 本次更新（v42）核心特性与修复

1. **拖拽性能重构与零延迟硬件加速 (src/components/CharacterList.tsx)**
   - **消除拖拽延迟与果冻效应**：彻底移除了拖拽过程中卡片容器自带的 	ransition-transform duration-150 过渡动画，避免每一帧鼠标/触控位移与 CSS 补间动画冲突导致的严重滞后感。
   - **GPU 3D 硬件加速**：将坐标变换全面切换至 CSS.Translate（	ranslate3d(x, y, 0)）并挂载 willChange: "transform" 与 	ouchAction: "none"，直接交由 GPU Compositor 渲染，卡片拖拽与排序实现 60/120 FPS 丝滑顺畅。
   - **触控与鼠标采样响应提速**：优化激活约束（Mouse 4px / Touch 180ms 6px），拖拽起手更快更灵敏。

2. **毛玻璃文件夹封面无封面文案更新 (src/components/FrostedFolderCover.tsx)**
   - 文件夹无图片时，卡片背景虚线框居中提示文案按需由「空文件夹」更新为「无封面」，视觉提示更准确。

3. **详情页聊天记录操作按钮全面换新 (src/components/CharacterChatsSection.tsx)**
   - **「清理」按钮**：采用全新胶囊药丸风格 ounded-full，带红色垃圾桶图标 Trash2 text-red-400，背景与边框采用高级磨砂微光质感 g-white/10 hover:bg-white/15 border border-white/15。
   - **「导入」按钮**：采用全新胶囊药丸风格 ounded-full，带紫色微光背景 g-purple-600/20 hover:bg-purple-600/30 border border-purple-500/30 text-purple-300。
   - **备忘录与版本迭代按钮**：同步保持 24 中的全部 ounded-full 胶囊药丸与毛玻璃微光材质。

4. **毛玻璃多图文件夹封面与专属封面选择器 (src/components/FrostedFolderCover.tsx, src/components/FolderCoverPickerModal.tsx, src/components/CharacterList.tsx)**
   - **全新毛玻璃微光封面 (FrostedFolderCover)**：采用层次分明的毛玻璃堆叠视觉效果，优雅呈现文件夹内前 4 张卡片的半透明浮动光影与卡面预览。
   - **文件夹专属封面选择器 (FolderCoverPickerModal)**：支持长按文件夹卡片一键呼出封面管理弹窗，可直接从卡库中挑选任意角色卡设为封面、从本地相册上传专属图片（支持 2:3 比例自由裁切与缩放），或随时一键恢复毛玻璃多图自动预览。

5. **瀑布流与网格卡片 2:3 黄金比例统一与视口异步加速 (src/components/CharacterList.tsx)**
   - 瀑布流与网格卡片全面锁定为 2:3 黄金比例（spect-[2/3]）。
   - 角色卡片全量引入 loading="lazy" 与 decoding="async"，极大降低图库滚动与渲染时的内存开销。

6. **跨版本关键特性完整保留与兼容**
   - **会话导入增强 (src/lib/chatParse.ts, src/components/CharacterChatsSection.tsx)**：完整保留多行 TXT 会话解析（支持中英文冒号 ：/ :）、JSONL 逐行解析与 ZIP 压缩包批量导入。
   - **云端卡库体验 (src/components/CloudSyncTab.tsx)**：卡片操作文案保持为「下载」，直调底层保存接口。
   - **底层真实时间戳解析 (src/lib/fileDate.ts, src/components/ImportModal.tsx)**：提取 PNG 	IME 块、EXIF 时间头与卡片内嵌元数据，解决 Android SAF 导入临时缓存导致时间被伪造的问题。
   - **跨目录翻页记忆 (src/components/CharacterList.tsx)**：独立记忆每个目录的分页位置（olderPageMemory）。

## 版本包说明
- **miu_v42**: 完整 Capacitor 项目工程（包含 android/ 目录与最新 dist/ 编译产物）。
- **miu_v42_web_干净版**: 纯净前端工程（纯 src/ 结构，无任何冗余根目录重复代码及缓存，轻量整洁）。
- **miu_v42_android_干净版**: Android Studio 干净构建包（已同步最新 Web 产物至 assets/public，无需 node 环境即可直接导入 Android Studio 打包 APK）。
