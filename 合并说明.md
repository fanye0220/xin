# miu_v40 更新与合并说明（基于 9月最新miu (9) 全面增强版）

在「9月最新miu (9)」及前期版本基础上完成完整功能合并、构建与打包：

## 本次更新（v40）核心特性与修复

1. **精准资源卡识别与统一角标视觉体系 (src/lib/cardBadge.ts, src/lib/cardType.ts, src/lib/db.ts)**
   - **严格甄别真实角色卡**：重构 isActualCharacterCard 与卡片分类识别逻辑，彻底杜绝真实角色卡被误识别为预设/世界书/工具/QR。
   - **轻量元数据分类索引**：char_meta 表直存 category 字段，并在首次加载时自动执行修复检查（REPAIR_CATEGORY_FLAG），自动校正历史版本中的误判数据并移出工具目录。
   - **统一圆点色彩角标**：新增 getCardBadgeInfo / getCardTypeBadgeInfo，在主页列表、网格瀑布流及回收站卡片上统一展示高质感圆点角标（快速回复-绿、预设-黄、脚本-青、世界书-紫、美化-粉、聊天记录-蓝）。

2. **版本迭代溯源中心性能与交互深度优化 (src/components/CharacterVersionsSection.tsx)**
   - **视口懒加载与内存自动回收**：关联候选卡片引入 CandidateAvatar 配合 IntersectionObserver 视口懒加载与 URL 自动释放，数千张卡片流畅滑动不掉帧。
   - **顶层 Portal 渲染**：所有关联、快照弹窗全面接入 createPortal(..., document.body)，彻底消除层级遮挡并增强系统返回键拦截。
   - **候选卡片严格过滤**：自动过滤所有非角色卡（预设、世界书、QR 等），仅保留纯粹角色卡供版本溯源绑定。

3. **弹窗层级与浅色主题全面适配 (src/components/BindQRModal.tsx, src/components/ExportQRModal.tsx, src/components/SelectQRModal.tsx)**
   - 快速回复绑定、导出及多选弹窗全面使用 createPortal 挂载至顶层，并完美优化浅色主题下的背景、文字及边框色彩对比度。

4. **导入解析分类鉴定强化 (src/components/ImportModal.tsx)**
   - 导入解析 JSON / PNG 时优先通过 isActualCharacterCard 鉴定，确保包含特定描述文本的角色卡不会被误分流至工具区。

5. **重复卡片检测功能强化 (src/components/DuplicateDetector.tsx)**
   - 引入合并锁定标识与精准分组检测，多级分类长路径自适应截断。

6. **云端同步与备份全面支持版本历史 (src/lib/cloudDrive.ts, src/lib/drive.ts, src/components/CloudSyncTab.tsx)**
   - Google Drive 卡库与全量备份完整支持打包、传输与恢复 ersionHistory 快照及对应卡面 PNG。
   - 卡库下载操作文案保持为「下载」，直调底层保存接口。

7. **底层真实时间戳解析与导入时间修复 (src/lib/fileDate.ts, src/components/ImportModal.tsx)**
   - 提取 PNG 	IME 块、EXIF 时间头与卡片内嵌元数据，解决 Android SAF 导入临时缓存导致时间被伪造的问题。

8. **跨版本关键特性完整保留与兼容**
   - **会话导入增强 (src/lib/chatParse.ts, src/components/CharacterChatsSection.tsx)**：完整保留多行 TXT 会话解析（支持中英文冒号 ：/ :）、JSONL 逐行解析与 ZIP 压缩包批量导入。
   - **拖拽移入文件夹与跨目录翻页记忆 (src/components/CharacterList.tsx)**：支持角色卡直接拖拽移入目标文件夹，独立记忆每个目录的分页位置（olderPageMemory）。

## 版本包说明
- **miu_v40**: 完整 Capacitor 项目工程（包含 android/ 目录与最新 dist/ 编译产物）。
- **miu_v40_web_干净版**: 纯净前端工程（纯 src/ 结构，无任何冗余根目录重复代码及缓存，轻量整洁）。
- **miu_v40_android_干净版**: Android Studio 干净构建包（已同步最新 Web 产物至 assets/public，无需 node 环境即可直接导入 Android Studio 打包 APK）。
