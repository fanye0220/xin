# miu_v42 更新与合并说明（基于 9月最新miu 9-26 视觉与封面重大升级版）

在「9月最新miu (24)」及前期版本基础上完成完整功能合并、生产构建与打包：

## 本次更新（v42）核心特性与修复

1. **毛玻璃多图文件夹封面与自定义封面选择器 (src/components/FrostedFolderCover.tsx, src/components/FolderCoverPickerModal.tsx, src/components/CharacterList.tsx)**
   - **全新毛玻璃微光封面 (FrostedFolderCover)**：采用层次分明的毛玻璃堆叠视觉效果，优雅呈现文件夹内前 4 张卡片的半透明浮动光影与卡面预览。
   - **文件夹专属封面选择器 (FolderCoverPickerModal)**：支持长按文件夹卡片一键呼出封面管理弹窗，可直接从卡库中挑选任意角色卡设为封面、从本地相册上传专属图片（支持 2:3 比例自由裁切与缩放），或随时一键恢复毛玻璃多图自动预览。

2. **全局 UI 视觉高级感全面升级 (src/components/CharacterVersionsSection.tsx, src/components/CharacterChatsSection.tsx, src/components/CharacterMemosSection.tsx)**
   - **高雅胶囊药丸设计**：全应用核心操作按钮（版本快照、关联旧版、聊天记录清理/导入、备忘录排序/新建等）全面升级为极具科技感与设计美学的圆角胶囊药丸（ounded-full）与微光磨砂质感（ackdrop-blur、g-white/10）。
   - **浅色与深色模式完美适应**：所有操作浮层与面板均进行深度色彩对比度适配，浅色模式下同样清爽细腻。

3. **瀑布流与网格卡片 2:3 黄金比例统一与视口异步加速 (src/components/CharacterList.tsx)**
   - 修复瀑布流与网格卡片尺寸参差不齐的问题，全面锁定为 2:3 黄金比例（spect-[2/3]）。
   - 角色卡片全量引入 loading="lazy" 与 decoding="async"，极大降低图库滚动与渲染时的内存开销。

4. **精准资源卡识别与统一角标视觉体系 (src/lib/cardBadge.ts, src/lib/cardType.ts, src/lib/db.ts)**
   - 重构 isActualCharacterCard 判定逻辑，杜绝包含特定描述字段的角色卡被误判为预设/世界书/工具/QR。
   - char_meta 索引表直存 category 字段，并在首次加载时自动执行修复检查（REPAIR_CATEGORY_FLAG），自动校正历史版本中的误判数据并将其移出工具分类目录。
   - 在角色列表、瀑布流网格及回收站卡片上统一展示高质感圆点角标（快速回复-绿、预设-黄、脚本-青、世界书-紫、美化-粉、聊天记录-蓝）。

5. **版本迭代溯源中心深度优化 (src/components/CharacterVersionsSection.tsx)**
   - 候选卡片引入 CandidateAvatar 配合 IntersectionObserver 视口懒加载与 URL 自动释放，数千张卡片流畅滑动不掉帧。
   - 所有关联、快照弹窗全面接入 createPortal(..., document.body)，彻底消除层级遮挡并增强系统返回键拦截。

6. **跨版本关键特性完整保留与兼容**
   - **会话导入增强 (src/lib/chatParse.ts, src/components/CharacterChatsSection.tsx)**：完整保留多行 TXT 会话解析（支持中英文冒号 ：/ :）、JSONL 逐行解析与 ZIP 压缩包批量导入。
   - **云端卡库体验 (src/components/CloudSyncTab.tsx)**：卡片操作文案保持为「下载」，直调底层保存接口。
   - **底层真实时间戳解析 (src/lib/fileDate.ts, src/components/ImportModal.tsx)**：提取 PNG 	IME 块、EXIF 时间头与卡片内嵌元数据，解决 Android SAF 导入临时缓存导致时间被伪造的问题。
   - **拖拽移入文件夹与跨目录翻页记忆 (src/components/CharacterList.tsx)**：支持角色卡直接拖拽移入目标文件夹，独立记忆每个目录的分页位置（olderPageMemory）。

## 版本包说明
- **miu_v42**: 完整 Capacitor 项目工程（包含 android/ 目录与最新 dist/ 编译产物）。
- **miu_v42_web_干净版**: 纯净前端工程（纯 src/ 结构，无任何冗余根目录重复代码及缓存，轻量整洁）。
- **miu_v42_android_干净版**: Android Studio 干净构建包（已同步最新 Web 产物至 assets/public，无需 node 环境即可直接导入 Android Studio 打包 APK）。
