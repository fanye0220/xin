# miu_v48 更新与合并说明（基于 9月最新miu 30 2K超清画质与云端路径解析版）

在「9月最新miu (30)」及前期版本基础上完成全量特性合并、2K超清缩略图生成与版本升级、云端路径智能解析与同名分类继承、生产构建打包：

## 本次更新（v48）核心特性与修复

1. **2K/3K 800px 超清缩略图生成器与版本升级缓存 (`src/lib/avatar.ts`, `src/lib/db.ts`)**
   - **800px 视网膜超清**：缩略图生成分辨率提升至 **800px（质量 0.90）**，在大屏手机、平板或高分辨率显示器上呈现极致细腻通透的画质。
   - **版本平滑升级机制**：`getCharacterThumb` 引入 `thumbVersion: 3` 标记检测，自动渐进式将本地历史低清缩略图在浏览时平滑无感地刷新为 800px 超清缩略图。

2. **云端路径智能解析与同名分类继承 (`src/lib/cloudDrive.ts`, `src/components/CloudSyncTab.tsx`)**
   - **独立解析引擎**：抽取封装 `resolveAppFolderFromCloudPath` 核心函数。
   - **自动剥离系统虚拟大类**：下载/恢复回 App 时，自动剥离云端专用的虚拟大类（`角色卡`、`工具区`、`聊天记录`）与工具二级分类，将真实用户文件夹精准还原回 App 本地。
   - **同名卡分类继承**：支持开启「同名卡自动跟随分类」后，自动继承本地已有同名卡的文件夹分类。

3. **封面裁剪器界面优化与 2:3 竖卡标准 (`src/components/CharacterList.tsx`)**
   - 统一固定为 2:3 标准竖卡比例，精简多余选项。
   - 适配移动端 Safe Area 安全区边距与响应式保存/取消按钮。

4. **IndexedDB 幽灵脏数据后台自动清除与启动秒开 (`src/lib/db.ts`, `src/App.tsx`)**
   - **`cleanupGhostCards` 自动清理**：启动后 500ms 异步后台检测数据库，精准扫描并清除无名称且无任何有效设定的无效幽灵卡，以及孤立的元数据与头像 Blob。
   - **启动秒开加速**：启动数据库迁移完毕后瞬间进入主页（`setIsMigrating(false)`），文件夹与幽灵卡清理全异步运行，启动体验极速顺滑。

5. **Web 端环境隔离与 Android 自动更新系统**
   - **Web 端纯净无扰**：自动隐藏设置中的「关于与更新」标签页，不触发任何更新检查弹窗。
   - **Android 在线更新**：保持对接 GitHub 仓库 `fanye0220/APP`，支持 3 秒自动静默检测与一键跳转 Releases 下载。

6. **历史关键特性全量完整保留**
   - **触控与拖拽硬件加速**：TouchSensor 250ms/8px 配合 `CSS.Translate` 极致跟手防误触。
   - **智能差异缓存防闪烁**：文件夹封面 Smart Memo，空文件夹文案保持为「无封面」。
   - **聊天记录提取与美化**：胶囊药丸按钮（“清理”带红色垃圾桶、“导入”带紫色微光），支持多行 TXT / JSONL / ZIP 解析。
   - **重复卡片免删锁定保护**：支持手动上锁，防误删防重复。
   - **云端下载文案**：云端卡库操作文案统一保持为「下载」。

## 版本包说明
- **miu_v48**: 完整 Capacitor 项目工程（包含 android/ 目录与最新 dist/ 编译产物）。
- **miu_v48_web_干净版**: 纯净前端工程（纯 src/ 结构，~378KB，轻量整洁，无更新弹窗干扰）。
- **miu_v48_android_干净版**: Android Studio 干净构建包（已同步最新 Web 产物至 assets/public，可直接打包 APK）。
- **GitHub更新文件_方便上传**: 包含可直接上传到 GitHub 仓库根目录的 `version.json` 与发布指南。
