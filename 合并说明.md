# miu_v46 更新与合并说明（基于 9月最新miu 28 幽灵卡清理与启动加速版）

在「9月最新miu (28)」及前期版本基础上完成全量特性合并、无效幽灵卡自动后台清除、启动秒开加速与生产构建打包：

## 本次更新（v46）核心特性与修复

1. **IndexedDB 幽灵脏数据后台自动清除 (`src/lib/db.ts`, `src/App.tsx`)**
   - **新增 `cleanupGhostCards`**：启动后 500ms 异步后台检测数据库，精准扫描并清除无名称且无任何有效设定的无效幽灵卡，以及孤立的元数据与头像 Blob。
   - **启动秒开加速**：将文件夹与幽灵卡清理移至后台异步运行，数据库迁移完毕后瞬间进入主页（`setIsMigrating(false)`），大幅缩短启动等待时间。

2. **自动打标器精准过滤优化 (`src/lib/taggerState.ts`)**
   - 自动过滤回收站卡片、工具区卡片（世界书/预设/快速回复/美化/脚本）以及无效空卡，确保 AI/规则自动打标器只对真实有效的角色卡执行打标。

3. **毛玻璃文件夹视觉对比度微调 (`src/components/FrostedFolderCover.tsx`)**
   - 虚线边框与背景半透明度微调（`bg-white/[0.04]`），无封面占位文字与图标微光质感更清晰自然。

4. **Web 端隔离与 Android 更新系统**
   - Web 端保持清爽（自动隐藏「关于与更新」标签页，不触发任何更新检测弹窗）。
   - Android 端保持在线自动更新检测（对接 GitHub 仓库 `fanye0220/APP`）。

5. **历史关键特性完整保留**
   - 云端备忘录与小剧场完整备份与恢复。
   - 重复卡片免删锁定保护与智能合并跳过。
   - 文件夹封面智能差异缓存防闪烁与精准 Memo 浅比较。
   - 详情页聊天记录操作按钮（胶囊药丸风格、“清理”带红色垃圾桶、“导入”带紫色微光）。
   - 多行 TXT / JSONL / ZIP 聊天记录解析引擎。
   - 云端卡库操作文案保持为「下载」。

## 版本包说明
- **miu_v46**: 完整 Capacitor 项目工程（包含 android/ 目录与最新 dist/ 编译产物）。
- **miu_v46_web_干净版**: 纯净前端工程（纯 src/ 结构，轻量整洁，无更新弹窗干扰）。
- **miu_v46_android_干净版**: Android Studio 干净构建包（已同步最新 Web 产物至 assets/public，可直接打包 APK）。
- **GitHub更新文件_方便上传**: 包含可直接上传到 GitHub 仓库根目录的 `version.json` 与发布指南。
